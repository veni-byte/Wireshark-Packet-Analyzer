# Wireshark Packet Size Visualizer

This beginner-friendly Python project uses **PyShark** and **Matplotlib** to analyze Wireshark `.pcap` files and visualize the size of each network packet.

## 🧠 What it Does
- Reads packets from a `.pcap` file captured using Wireshark  
- Extracts the size (in bytes) of each packet  
- Plots a line graph showing how packet sizes vary  

## 🧰 Technologies Used
- Python  
- PyShark  
- Matplotlib  
- Nest AsyncIO  

## 🚀 How to Run
1. Install dependencies:
   ```bash
   pip install -r requirements.txt
